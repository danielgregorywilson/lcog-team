import { DeskReservationStateInterface } from 'src/store/types';

const state: DeskReservationStateInterface = {
  allDesks: { results: [] },
  allDeskReservations: { results: [] }
};

export default state;
