import { PeopleStateInterface } from 'src/store/types';

const state: PeopleStateInterface = {
  simpleEmployeeList: [],
  simpleEmployeeDetail: { pk: -1, name: '' },
  unitList: [],
};

export default state;
